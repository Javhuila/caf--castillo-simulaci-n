const express= require('express');
const router= express.Router();
const MinifacController=require('../../controladores/minic/MinifacController');

/*router.get('/muro/:id_usuario/:id_post',MinifacController.consultarpost);*/
